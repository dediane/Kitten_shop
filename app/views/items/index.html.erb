<div class="jumbotron" style="text-align:center">
  <h1 class="display-3"><i class="fas fa-cat fa-1x pr-4" style="color:black" ></i>Kitten Shop</h1>
  <p class="lead">Enfin un a-chat qui va avoir du sens ! 🐾</p>
  <hr class="my-4">
  <p>Pour passer à l'action et 🐾 vous offrir l'une de nos super photos de chats, 🐾 parcourez notre page ci-dessous,</p>
  <p>et choisissez 🐾  le ou les chats qui vous font plaisir ! 🐾</p>
</div>

 <div class ="row col">
  <% @items.each do |item| %>
    <div class="card mb-3 m-auto" style="width:18rem;">
      <h3 class="card-header"><%= item.title %></h3>
      <%if item.picture.attached?%>
        <%= image_tag item.picture, alt: 'photo de chat', class: "m-3" %>
      <%else%>
        <p>aucune photo de chat</p>
      <%end%>
      
      <div class="card-body">
        <p class="card-text"> <%= item.description %></p>
      </div>
      <div>
        <span class="card-price"> <%= item.price %> </span>
        <span class ="symbol">€</span>
      </div>
      <div class="card-body">
        <%= link_to 'Infos', item, class: "btn btn-outline-primary" %>
        <% if current_user != nil %>
          <%= link_to 'Ajouter au panier', line_items_path(item_id: item.id), method: :post, class: "btn btn-outline-success" %>
        <% else %>
          <%= link_to 'Ajouter au panier', new_user_session_path, method: :get, class: "btn btn-outline-success" %>
        <% end %>
        <% if is_current_user_admin? %>
          <%= link_to 'Editer', edit_item_path(id: item.id), method: :get, class: "btn btn-outline-secondary" %>
        <% end %>
      </div>
      <div class="fronter">  
          _________________________________
      </div>
    </div>
  <% end %>
</div>
